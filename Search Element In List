#include <stdio.h>

int main() {
    int N, search_element, found = 0;

    // Read the size of the list
    scanf("%d", &N);

    int elements[N];

    // Read the elements of the list
    for (int i = 0; i < N; i++) {
        scanf("%d", &elements[i]);
    }

    // Read the searching element
    scanf("%d", &search_element);

    // Check if the element is in the list
    for (int i = 0; i < N; i++) {
        if (elements[i] == search_element) {
            found = 1;
            break;
        }
    }

    if (found) {
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }

    return 0;
}
